<!-- GENERATED by `cargo xtask validate` from commit `17dd604-dirty`. Do not edit by hand: the next run overwrites it. -->

# Validation, measured

> **This page is generated.** `cargo xtask validate` wrote it from commit `17dd604-dirty`.
> Every number on it was captured from that run's own output. To change what it says,
> change what the suite measures and run the command again.

The narrative version of this material, with the reasoning behind each tier and the
history of what it caught, is in [Validation](../validation.md). This section is the
other half: the numbers, written by the command that ran the suite, from the suite's
own output. Nothing here was transcribed.

A tier with no chapter has not been generated. That is stated rather than left to be
inferred from an absence, because a missing page and a page nobody updated look the
same from the table of contents.

Toolchain on the machine that ran this: rustc 1.97.1 (8bab26f4f 2026-07-14), gfortran 15.2.0.

## Fidelity preflight

`cargo xtask fidelity` runs the port forward from the nominal state and diffs states,
derivatives, measurements and the generator word against a golden oracle trace
committed to the repository. It needs no Fortran toolchain, so it runs everywhere in
about a second.

| steps diffed | worst | where | gate | trace recorded with |
|---|---|---|---|---|
| 100 of 100 | 3.521323734565789e-14 | `YP(12) at step 77` | 1e-12 | gfortran 15.2.0 |

## The ladder

| tier | what it proves | chapter |
|---|---|---|
| 1 | `TESUB1` to `TESUB8` match the oracle | [tier 1](tier1.md), from `17dd604-dirty` by `cargo xtask validate --tiers 1,2,3 --smoke` |
| 2 | single-step derivatives match, over all three pools | [tier 2](tier2.md), from `17dd604-dirty` by `cargo xtask validate --tiers 1,2,3 --smoke` |
| 3 | the generator call *order* matches, draw for draw | [tier 3](tier3.md), from `17dd604-dirty` by `cargo xtask validate --tiers 1,2,3 --smoke` |
| 4 | trajectories stay inside the measurement noise (diagnostic) | runs, but writes no chapter yet |
| 5 | statistical equivalence: TOST, KS, ACF, spectra, correlations | runs, but writes no chapter yet |
| 6 | downstream detectors cannot tell the two sources apart | no harness yet |
| 7 | the published `d00` to `d21` files are reproduced | no harness yet |
| 8 | differential fuzzing finds no counterexample | no harness yet |
| 9 | identical digests across platforms, wasm included | no harness yet |
| 10 | every quirk fix ships with a measured delta | no harness yet |

This run selected tier(s) [1, 2, 3] and wrote chapter(s) for [1, 2, 3]. Tiers 4 and 5
run but do not write a chapter yet; tiers 6 to 10 have no harness. The [delta
index](delta-index.md) is generated separately, by `cargo xtask deltas`.

