//! Python bindings.
//!
//! Empty on purpose: PyO3 arrives in phase 7. The crate exists now so the
//! workspace layout is fixed and the licence metadata is uniform. Adding the
//! dependency early would slow every gate run for no benefit.
//!
//! # Status
//!
//! Skeleton. Lands in phase 7; see `BACKLOG.org`.

#![forbid(unsafe_code)]
