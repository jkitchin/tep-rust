# Generated by `wasm-bindgen --target web --out-dir crates/tepsim-wasm/www/pkg`.
# Build output, tied to one wasm-bindgen version and one build profile, so it is
# regenerated rather than committed. The command is in index.html.
pkg/
